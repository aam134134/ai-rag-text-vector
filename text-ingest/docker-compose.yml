services:
  file-watcher:
    image: ${PROJECT_NAME}:${PROJECT_VERSION}
    build:
      context: .
      dockerfile: Dockerfile
      args:
         PROJECT_NAME: ${PROJECT_NAME}
    volumes:
      - ./data:/app/data:rw
    environment:
      WATCH_DIR: ${APP_HOME}/data/ingest/inbox
      ARCHIVE_DIR: ${APP_HOME}/data/ingest/archive
      CHUNKS_DIR: ${APP_HOME}/data/ingest/chunks
    container_name: "${PROJECT_NAME}" 
    tty: true
    stdin_open: true

